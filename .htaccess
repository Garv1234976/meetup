RewriteEngine On

# Block .builds
RewriteRule ^\.builds - [F,L]

# If file exists → serve it
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# If directory exists → serve it
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Otherwise → React SPA fallback
RewriteRule ^ index.html [L]